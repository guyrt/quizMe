version: '3'

services:
#  web:
#    restart: always
#    build:
#      context: ./
#      dockerfile: ./webserve/Dockerfile
#    expose:
#      - "8000"
#    links:
#      - postgres:postgres
#      - redis:redis
#    volumes:
#      - webserve-django:/usr/src/app
#      - webserve-static:/usr/src/app/static
#    env_file: .env
#    environment:
#      DEBUG: 'true'
#    command: /usr/local/bin/gunicorn docker_django.wsgi:application -w 2 -b :8000
#
#
#  nginx:
#    # https://hub.docker.com/_/nginx/
#    restart: always
#    build: ./nginx/
#    ports:
#      - "80:80"
#    volumes:
#      - webserve-static:/www/static
#    links:
#      - web:web
#    # environment:
#      # this img support env if needed
#      # - NGINX_HOST=foobar.com
#      # - NGINX_PORT=80

  pgsql:
    image: pgvector/pgvector:pg15
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgressu
      - POSTGRES_PASSWORD=mosdumps123
    ports:
      - "5432:5432"

  redis:
    restart: always
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
#  webserve-django:
#  webserve-static:
  pgdata:
  redisdata: